import RegisterForm from "../(auth)/_component/register-form";

export default function RegisterPage() {
  return (
    <div className="flex items-center justify-center">
      <RegisterForm />
    </div>
  );
}